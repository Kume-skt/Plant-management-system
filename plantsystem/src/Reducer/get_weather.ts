//Actionの処理
const reData = (state = "adfafiafja", action: { type: string; }) => {
   switch (action.type) {
      case 'Weather':
          return 

      default:
          return state
   }
};
export default reData;